export enum EnvKeys {
  ENV = 'environment',
  PORT = 'port',
  DB_NAME = 'db_name',
  DB_PASSWORD = 'db_password',
  DB_USERNAME = 'db_username',
  DB_HOST = 'db_host',
  DB_PORT = 'db_port',
  JWT_SECRET = 'jwt_secret',
}

export enum Environments {
  DEV = 'dev',
  PROD = 'prod',
}
